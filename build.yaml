targets:
  $default:
    builders:
      freezed:
        generate_for:
          - lib/**/models/**
      json_serializable:
        options:
          any_map: true
        generate_for:
          - lib/**/models/**
